# 협업 후기 분석 시스템 환경 구축 가이드

## 📋 목차
1. [리눅스 서버 환경 설정](#1-리눅스-서버-환경-설정)
2. [Google Cloud 연동 설정](#2-google-cloud-연동-설정)
3. [개발 도구 설정](#3-개발-도구-설정)
4. [프로젝트 환경 구성](#4-프로젝트-환경-구성)
5. [테스트 및 검증](#5-테스트-및-검증)

---

## 1. 리눅스 서버 환경 설정

### 1.1 Ubuntu 운영체제 준비

#### 1.1.1 시스템 요구사항
- Ubuntu 20.04 LTS 이상
- 최소 메모리: 8GB RAM
- 저장공간: 50GB 이상
- 인터넷 연결 필수

#### 1.1.2 Ubuntu 설치 방법

**옵션 1: 물리 서버 설치**
1. Ubuntu 20.04 LTS ISO 파일 다운로드
   ```
   https://ubuntu.com/download/server
   ```
2. 부팅 USB 생성 후 서버에 설치
3. 기본 설정 완료 (사용자 계정, 네트워크 등)

**옵션 2: 가상머신 설치 (VMware/VirtualBox)**
1. VMware Workstation 또는 VirtualBox 설치
2. 새 가상머신 생성 (메모리 8GB, 디스크 50GB)
3. Ubuntu Server ISO로 부팅하여 설치

**옵션 3: 클라우드 서버 (AWS/GCP/Azure)**
1. 클라우드 제공업체에서 Ubuntu 인스턴스 생성
2. 적절한 인스턴스 타입 선택 (최소 2vCPU, 8GB RAM)
3. SSH 키 설정 및 보안 그룹 구성

### 1.2 기본 시스템 설정

#### 1.2.1 시스템 업데이트
```bash
# 패키지 목록 업데이트
sudo apt update

# 시스템 패키지 업그레이드
sudo apt upgrade -y

# 필수 도구 설치
sudo apt install -y curl wget git vim htop
```

#### 1.2.2 방화벽 설정
```bash
# UFW 방화벽 활성화
sudo ufw enable

# SSH 접속 허용 (포트 22)
sudo ufw allow ssh

# HTTP/HTTPS 허용 (필요시)
sudo ufw allow 80
sudo ufw allow 443

# 상태 확인
sudo ufw status
```

### 1.3 Python 환경 구성

#### 1.3.1 Python 3.9+ 설치
```bash
# Python 버전 확인
python3 --version

# Python 3.9가 없는 경우 설치
sudo apt install -y python3.9 python3.9-dev python3.9-venv

# pip 설치
sudo apt install -y python3-pip

# pip 업그레이드
python3 -m pip install --upgrade pip
```

#### 1.3.2 가상환경 설정
```bash
# 프로젝트 디렉토리 생성
mkdir -p ~/team-review-analysis
cd ~/team-review-analysis

# Python 가상환경 생성
python3 -m venv venv

# 가상환경 활성화
source venv/bin/activate

# 가상환경 활성화 확인 (프롬프트에 (venv) 표시됨)
```

#### 1.3.3 필수 라이브러리 설치
```bash
# 가상환경 활성화 상태에서 실행
pip install pandas==2.0.3
pip install google-cloud-aiplatform==1.38.1
pip install scikit-learn==1.3.0
pip install openpyxl==3.1.2
pip install matplotlib==3.7.2
pip install seaborn==0.12.2
pip install jupyter==1.0.0

# 또는 requirements.txt 파일 사용 (파일이 있는 경우)
pip install -r requirements.txt
```

---

## 2. Google Cloud 연동 설정

### 2.1 Google Cloud 프로젝트 생성

#### 2.1.1 Google Cloud Console 접속
1. https://console.cloud.google.com 접속
2. Google 계정으로 로그인
3. 새 프로젝트 생성 또는 기존 프로젝트 선택

#### 2.1.2 프로젝트 설정
```
프로젝트 이름: team-review-analysis (또는 원하는 이름)
프로젝트 ID: 자동 생성된 ID 사용
결제 계정: 유효한 결제 방법 연결 필요
```

### 2.2 Vertex AI API 활성화

#### 2.2.1 API 활성화 방법
1. Google Cloud Console에서 "API 및 서비스" → "라이브러리" 이동
2. "Vertex AI API" 검색
3. "사용 설정" 클릭
4. 활성화 완료까지 대기 (보통 1-2분)

#### 2.2.2 필요한 추가 API들
```
- Vertex AI API
- Cloud Resource Manager API
- Identity and Access Management (IAM) API
```

### 2.3 서비스 계정 생성 및 인증

#### 2.3.1 서비스 계정 생성
1. "IAM 및 관리자" → "서비스 계정" 이동
2. "서비스 계정 만들기" 클릭
3. 서비스 계정 정보 입력:
   ```
   이름: team-review-ai-service
   설명: 협업후기 AI 분석을 위한 서비스 계정
   ```

#### 2.3.2 권한 부여
서비스 계정에 다음 역할 부여:
```
- Vertex AI User
- AI Platform Developer
- Storage Object Viewer (필요시)
```

#### 2.3.3 인증 키 생성
1. 생성된 서비스 계정 클릭
2. "키" 탭으로 이동
3. "키 추가" → "새 키 만들기"
4. JSON 형식 선택
5. 다운로드된 JSON 파일을 안전한 위치에 저장

### 2.4 서버에서 인증 설정

#### 2.4.1 인증 파일 업로드
```bash
# 로컬에서 서버로 인증 파일 복사 (scp 사용)
scp path/to/your-service-account-key.json username@server-ip:~/team-review-analysis/

# 또는 서버에서 직접 생성하여 내용 복사
nano ~/team-review-analysis/service-account-key.json
# JSON 내용을 복사하여 붙여넣기
```

#### 2.4.2 환경 변수 설정
```bash
# .bashrc 파일 편집
nano ~/.bashrc

# 파일 끝에 다음 라인 추가
export GOOGLE_APPLICATION_CREDENTIALS="$HOME/team-review-analysis/service-account-key.json"
export GOOGLE_CLOUD_PROJECT="your-project-id"

# 변경사항 적용
source ~/.bashrc

# 환경 변수 확인
echo $GOOGLE_APPLICATION_CREDENTIALS
echo $GOOGLE_CLOUD_PROJECT
```

#### 2.4.3 인증 테스트
```bash
# 가상환경 활성화
source ~/team-review-analysis/venv/bin/activate

# Python에서 인증 테스트
python3 -c "
from google.cloud import aiplatform
aiplatform.init(project='your-project-id', location='us-central1')
print('Google Cloud 인증 성공!')
"
```

---

## 3. 개발 도구 설정

### 3.1 Cursor 편집기 설치

#### 3.1.1 Cursor 다운로드 및 설치
```bash
# Cursor 공식 웹사이트에서 Linux 버전 다운로드
wget https://download.cursor.sh/linux/appImage/x64 -O cursor.AppImage

# 실행 권한 부여
chmod +x cursor.AppImage

# Cursor 실행
./cursor.AppImage
```

#### 3.1.2 Cursor 설정
1. 처음 실행 시 계정 로그인 (또는 스킵)
2. Python 확장 프로그램 설치
3. 프로젝트 폴더 열기: `~/team-review-analysis`

### 3.2 Jupyter Notebook 설정

#### 3.2.1 Jupyter 설치 및 실행
```bash
# 가상환경에서 Jupyter 설치 (이미 설치됨)
pip install jupyter

# Jupyter 설정 파일 생성
jupyter notebook --generate-config

# 원격 접속을 위한 설정 (필요시)
nano ~/.jupyter/jupyter_notebook_config.py
```

#### 3.2.2 Jupyter 설정 (원격 접속용)
```python
# jupyter_notebook_config.py 파일에 추가
c.NotebookApp.ip = '0.0.0.0'
c.NotebookApp.port = 8888
c.NotebookApp.open_browser = False
c.NotebookApp.allow_root = True
```

### 3.3 Git 버전 관리 설정

#### 3.3.1 Git 초기 설정
```bash
# Git 사용자 정보 설정
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# 프로젝트 Git 저장소 초기화
cd ~/team-review-analysis
git init

# .gitignore 파일 생성
nano .gitignore
```

#### 3.3.2 .gitignore 설정
```
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# 가상환경
venv/
env/
ENV/

# Jupyter Notebook
.ipynb_checkpoints

# 인증 파일
service-account-key.json
*.json

# 데이터 파일
*.xlsx
*.csv
*.json

# IDE
.vscode/
.idea/
```

---

## 4. 프로젝트 환경 구성

### 4.1 프로젝트 구조 생성

#### 4.1.1 디렉토리 구조 생성
```bash
cd ~/team-review-analysis

# 프로젝트 구조 생성
mkdir -p {data,src,notebooks,results,docs}

# 구조 확인
tree
```

#### 4.1.2 프로젝트 구조 설명
```
team-review-analysis/
├── data/                   # 원본 데이터 파일
├── src/                    # Python 소스 코드
├── notebooks/              # Jupyter 노트북
├── results/                # 분석 결과 파일
├── docs/                   # 문서
├── venv/                   # 가상환경
├── service-account-key.json # Google Cloud 인증
├── requirements.txt        # 패키지 의존성
└── README.md              # 프로젝트 설명
```

### 4.2 requirements.txt 생성

#### 4.2.1 의존성 파일 생성
```bash
# 현재 설치된 패키지 목록을 requirements.txt로 저장
pip freeze > requirements.txt

# 또는 수동으로 생성
nano requirements.txt
```

#### 4.2.2 requirements.txt 내용
```
pandas==2.0.3
google-cloud-aiplatform==1.38.1
scikit-learn==1.3.0
openpyxl==3.1.2
matplotlib==3.7.2
seaborn==0.12.2
jupyter==1.0.0
numpy==1.24.3
```

### 4.3 환경 변수 설정

#### 4.3.1 .env 파일 생성
```bash
nano .env
```

#### 4.3.2 .env 파일 내용
```
# Google Cloud 설정
GOOGLE_APPLICATION_CREDENTIALS=./service-account-key.json
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# 프로젝트 설정
PROJECT_ROOT=/home/username/team-review-analysis
DATA_DIR=./data
RESULTS_DIR=./results

# AI 모델 설정
AI_MODEL=gemini-2.0-flash-exp
MAX_TOKENS=8192
TEMPERATURE=0.1
```

---

## 5. 테스트 및 검증

### 5.1 환경 설정 검증

#### 5.1.1 Python 환경 테스트
```bash
# 가상환경 활성화
source venv/bin/activate

# Python 버전 확인
python --version

# 필수 패키지 import 테스트
python -c "
import pandas as pd
import numpy as np
from google.cloud import aiplatform
import sklearn
print('모든 패키지 import 성공!')
print(f'pandas: {pd.__version__}')
print(f'numpy: {np.__version__}')
print(f'scikit-learn: {sklearn.__version__}')
"
```

#### 5.1.2 Google Cloud 연결 테스트
```bash
# Google Cloud 인증 상태 확인
gcloud auth list

# Vertex AI 접속 테스트
python -c "
from google.cloud import aiplatform
import os

project_id = os.getenv('GOOGLE_CLOUD_PROJECT')
location = os.getenv('GOOGLE_CLOUD_LOCATION', 'us-central1')

aiplatform.init(project=project_id, location=location)
print(f'프로젝트: {project_id}')
print(f'지역: {location}')
print('Vertex AI 연결 성공!')
"
```

### 5.2 샘플 데이터 테스트

#### 5.2.1 테스트 데이터 생성
```python
# test_data.py 파일 생성
import pandas as pd

# 샘플 데이터 생성
test_data = pd.DataFrame({
    'id': range(1, 6),
    'text': [
        '항상 친절하게 응대해주셔서 감사합니다.',
        '업무 처리가 늦어서 불편했습니다.',
        '전문적인 조언을 받을 수 있어서 좋았습니다.',
        '담당자를 찾기가 어려웠습니다.',
        '협업이 잘 이루어지고 있습니다.'
    ]
})

# CSV 파일로 저장
test_data.to_csv('data/sample_data.csv', index=False, encoding='utf-8')
print('샘플 데이터 생성 완료')
```

#### 5.2.2 AI 분석 테스트
```python
# test_ai.py 파일 생성
from google.cloud import aiplatform
import pandas as pd
import os

# 환경 설정
project_id = os.getenv('GOOGLE_CLOUD_PROJECT')
location = os.getenv('GOOGLE_CLOUD_LOCATION', 'us-central1')
aiplatform.init(project=project_id, location=location)

# 테스트 텍스트
test_text = "항상 친절하게 응대해주셔서 감사합니다."

# 간단한 감정 분석 테스트
prompt = f"""
다음 텍스트의 감정을 분석해주세요:
텍스트: {test_text}

결과를 다음 JSON 형식으로 제공해주세요:
{{
    "sentiment": "긍정/부정/중립",
    "confidence": 0.0-1.0,
    "keywords": ["키워드1", "키워드2"]
}}
"""

try:
    # Gemini 모델 테스트
    model = aiplatform.generative_models.GenerativeModel("gemini-2.0-flash-exp")
    response = model.generate_content(prompt)
    print("AI 분석 테스트 성공!")
    print(response.text)
except Exception as e:
    print(f"AI 분석 테스트 실패: {e}")
```

### 5.3 성능 테스트

#### 5.3.1 시스템 리소스 확인
```bash
# CPU 정보
lscpu

# 메모리 정보
free -h

# 디스크 공간 확인
df -h

# 시스템 부하 모니터링
htop
```

#### 5.3.2 네트워크 연결 테스트
```bash
# Google Cloud 연결 테스트
ping -c 4 8.8.8.8

# Vertex AI 엔드포인트 연결 테스트
curl -I https://us-central1-aiplatform.googleapis.com
```

---

## 6. 문제 해결 가이드

### 6.1 일반적인 문제 및 해결책

#### 6.1.1 Python 패키지 설치 오류
```bash
# pip 업그레이드
python -m pip install --upgrade pip

# 캐시 정리 후 재설치
pip cache purge
pip install --no-cache-dir package-name

# 시스템 패키지 의존성 설치
sudo apt install python3-dev build-essential
```

#### 6.1.2 Google Cloud 인증 오류
```bash
# 서비스 계정 키 파일 권한 확인
chmod 600 service-account-key.json

# 환경 변수 재설정
export GOOGLE_APPLICATION_CREDENTIALS="$(pwd)/service-account-key.json"

# gcloud 인증 (필요시)
gcloud auth activate-service-account --key-file=service-account-key.json
```

#### 6.1.3 메모리 부족 오류
```bash
# 스왑 파일 생성 (4GB)
sudo fallocate -l 4G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile

# 영구 설정
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
```

### 6.2 로그 및 모니터링

#### 6.2.1 시스템 로그 확인
```bash
# 시스템 로그
sudo journalctl -f

# Python 애플리케이션 로그
tail -f ~/.local/share/jupyter/jupyter.log
```

#### 6.2.2 리소스 모니터링
```bash
# 실시간 시스템 모니터링
htop

# 디스크 사용량 모니터링
watch df -h

# 네트워크 연결 상태
netstat -tulpn
```

---

## 7. 백업 및 보안

### 7.1 데이터 백업

#### 7.1.1 프로젝트 백업 스크립트
```bash
#!/bin/bash
# backup.sh

DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="$HOME/backups"
PROJECT_DIR="$HOME/team-review-analysis"

mkdir -p $BACKUP_DIR

# 프로젝트 백업 (인증 파일 제외)
tar --exclude='venv' --exclude='*.json' --exclude='__pycache__' \
    -czf $BACKUP_DIR/team-review-analysis_$DATE.tar.gz \
    -C $HOME team-review-analysis

echo "백업 완료: $BACKUP_DIR/team-review-analysis_$DATE.tar.gz"
```

### 7.2 보안 설정

#### 7.2.1 파일 권한 설정
```bash
# 인증 파일 보안 설정
chmod 600 service-account-key.json
chmod 600 .env

# 프로젝트 디렉토리 권한
chmod 755 ~/team-review-analysis
chmod -R 644 ~/team-review-analysis/*.py
```

#### 7.2.2 방화벽 규칙
```bash
# 필요한 포트만 열기
sudo ufw allow 22    # SSH
sudo ufw allow 8888  # Jupyter (필요시)
sudo ufw deny 80     # HTTP 차단 (필요시)
sudo ufw enable
```

---

이 가이드를 따라 환경을 구축하면 협업 후기 분석 시스템을 위한 완전한 개발 환경이 준비됩니다.

**문서 작성일**: 2025년 6월 24일  
**작성자**: [담당자명]  
**문서 버전**: 1.0