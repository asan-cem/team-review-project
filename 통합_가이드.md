# 서울아산병원 협업평가 시스템 - 통합 가이드

> AI 기반 의료진 협업 피드백 분석 및 대시보드 시스템

**버전**: 3.3  
**최종 업데이트**: 2025-10-22  
**Python 버전**: 3.8+

---

## 📚 목차

### 🔥 필수 (제로베이스 사용자 순서대로)
1. [프로젝트 소개](#-프로젝트-소개)
2. [시작하기 전에](#-시작하기-전에)
3. [개발 환경 설정](#-개발-환경-설정)
   - [WSL 설치 (Windows 사용자)](#-wsl-설치-windows-사용자-필수)
   - [Python 설치](#-python-설치-및-설정)
   - [Git 설치](#-git-설치-및-설정)
   - [프로젝트 환경 설정](#-프로젝트-환경-설정)
4. [데이터 준비 및 API 설정](#-데이터-준비-및-api-설정)
5. [프로젝트 구조 및 파일 설명](#-프로젝트-구조-및-파일-설명)
6. [사용 방법](#-사용-방법)
7. [스크립트별 상세 설명](#-스크립트별-상세-설명)
8. [일반적인 작업 흐름](#-일반적인-작업-흐름)

### 📖 참고 자료
9. [문제 해결 및 트러블슈팅](#-문제-해결-및-트러블슈팅)
10. [체크리스트](#-체크리스트)
11. [부록](#-부록)

---

## 🎯 프로젝트 소개

서울아산병원 협업평가 시스템은 의료진 간 협업 피드백을 AI로 분석하고 인터랙티브 대시보드를 생성하는 시스템입니다.

### 주요 기능

- ✅ **AI 감정 분석**: Google Gemini API를 통한 텍스트 감정 분석
- ✅ **대시보드 생성**: 인터랙티브 HTML 대시보드 (연도별/반기별)
- ✅ **부서별 보고서**: 개별 부서 상세 보고서 생성
- ✅ **통계 분석**: 연도별/반기별 통계 리포트
- ✅ **외부망 대응**: 인터넷 연결 없이도 그래프 표시

### 핵심 스크립트

| 스크립트 | 기능 | 출력 |
|---------|------|------|
| `0. 데이터_정리.py` | 원본 데이터 수집 및 정제 | `rawdata/1. data_processor_결과_*.xlsx` |
| `0. 주관식_정제.py` | AI 기반 텍스트 감정 분석 | `rawdata/2. text_processor_결과_*.xlsx` |
| `1. 대시보드_연도_반기.py` | 통합 대시보드 생성 (연도별/반기별) | `*.html` |
| `4. 연도별_통계분석.py` | 연도별 통계 분석 | `연도별_통계분석.xlsx` |
| `5. 반기별_통계분석.py` | 반기별 통계 분석 | `반기별_통계분석.xlsx` |
| `6. 상호평가_부서추출_연도_반기.py` | 부서별 상호평가 데이터 추출 | `상호평가_부서추출*.xlsx` |
| `7. 진단_데이터정제표.py` | 데이터 품질 진단 | `진단_데이터정제표.xlsx` |

### 기술 스택

- **언어**: Python 3.8+
- **데이터 처리**: pandas, numpy
- **시각화**: plotly
- **AI**: Google Gemini API
- **환경**: WSL2 (Ubuntu), VS Code / Cursor

---

## ⚠️ 시작하기 전에

이 프로젝트는 **Python 기반**입니다. Windows 사용자는 **WSL + Ubuntu**를 사용하는 것을 강력히 권장합니다.

### 설치 순서 (중요!)

1. ✅ **WSL + Ubuntu 24.04 LTS 설치** (Windows 사용자 필수)
2. ✅ **Python 3.8+ 설치** (필수)
3. ✅ **Git 설치** (필수, 버전 관리용)
4. ✅ **프로젝트 패키지 설치** (requirements.txt)
5. ✅ **데이터 파일 준비** (rawdata 폴더)
6. ✅ **API 키 설정** (Gemini API.json)

### 전제 조건

- Python 3.8 이상
- Google Gemini API 인증 파일 (`Gemini API.json`)
- 처리된 데이터 파일 (`rawdata/2. text_processor_결과_*.xlsx`) 또는 원본 데이터

---

## 🛠️ 개발 환경 설정

### 🪟 WSL 설치 (Windows 사용자 필수)

> 💡 **Windows 사용자 필수!** WSL을 통해 Ubuntu를 설치하면 Linux 환경에서 개발할 수 있습니다.

#### WSL이란?
Windows에서 Linux 운영체제를 실행할 수 있게 해주는 기능입니다. 가상머신보다 가볍고 빠릅니다.

#### 시스템 요구사항
- Windows 10 버전 2004 이상 (빌드 19041 이상)
- Windows 11 (모든 버전)

#### Step 1: Windows 버전 확인
```powershell
# Windows PowerShell 또는 명령 프롬프트에서 실행
winver

# Windows 10: 빌드 19041 이상이어야 함
# Windows 11: 모든 버전 가능
```

#### Step 2: WSL 설치 (가장 간단한 방법)
```powershell
# PowerShell을 관리자 권한으로 실행 (중요!)
# 시작 메뉴 → PowerShell 우클릭 → "관리자 권한으로 실행"

# WSL 및 Ubuntu 24.04 한 번에 설치
wsl --install -d Ubuntu-24.04

# 설치 완료 후 컴퓨터 재부팅
```

#### Step 3: Ubuntu 초기 설정
```bash
# 재부팅 후 Ubuntu가 자동으로 실행됩니다
# (실행되지 않으면 시작 메뉴에서 "Ubuntu 24.04" 검색하여 실행)

# 1. 사용자 계정 생성
# Unix username: 영문 소문자로 입력 (예: user, myname)
# Password: 비밀번호 입력 (화면에 표시되지 않음)
# Retype password: 비밀번호 재입력

# 2. 설치 확인
lsb_release -a
# 출력 예시:
# Description:    Ubuntu 24.04.x LTS
# Release:        24.04
```

#### Step 4: WSL 기본 설정
```powershell
# PowerShell에서 (관리자 권한)

# WSL 버전 확인
wsl --list --verbose

# Ubuntu가 WSL 2로 실행되는지 확인
# VERSION이 2여야 함. 1이면 다음 명령어 실행:
wsl --set-version Ubuntu-24.04 2

# 기본 배포판으로 설정
wsl --set-default Ubuntu-24.04
```

#### Step 5: Ubuntu 업데이트
```bash
# Ubuntu 터미널에서 실행

# 패키지 목록 업데이트
sudo apt update

# 시스템 업그레이드
sudo apt upgrade -y

# 완료 확인
echo "WSL Ubuntu 24.04 설치 완료!"
```

#### Windows에서 Ubuntu 파일 접근
```
파일 탐색기 주소창에 입력:
\\wsl$\Ubuntu-24.04\home\사용자명\

또는 Ubuntu 터미널에서:
explorer.exe .
```

---

### 🐍 Python 설치 및 설정

> ⭐ **가장 중요!** 협업평가 시스템은 Python으로 작성되었습니다.

> 💡 **전제 조건**: WSL Ubuntu 24.04 LTS가 설치되어 있어야 합니다. (Windows 사용자)

#### Python 설치 확인
```bash
# Ubuntu 터미널에서
python3 --version

# Python이 없으면 다음 단계로 진행
```

#### Step 1: 시스템 업데이트 (필수)
```bash
# 패키지 목록 업데이트
sudo apt update

# 기존 패키지 업그레이드 (보안 패치 포함)
sudo apt upgrade -y
```

#### Step 2: Python 3 설치
```bash
# Python 3 설치 (Ubuntu 24.04는 기본적으로 Python 3.12 포함)
sudo apt install -y python3

# 설치 확인
python3 --version
# 예상 출력: Python 3.12.x
```

#### Step 3: pip (패키지 관리자) 설치
```bash
# pip 설치
sudo apt install -y python3-pip

# 설치 확인
pip3 --version
# 예상 출력: pip 24.0 from /usr/lib/python3/dist-packages/pip (python 3.12)
```

#### Step 4: 가상환경 도구 설치
```bash
# python3-venv 설치 (가상환경 생성에 필수)
sudo apt install -y python3-venv

# 설치 확인
python3 -m venv --help
# 도움말이 출력되면 성공
```

#### Step 5: Python 개발 도구 설치
```bash
# Python 개발 헤더 파일 (일부 패키지 컴파일에 필요)
sudo apt install -y python3-dev

# C/C++ 컴파일러 및 빌드 도구
sudo apt install -y build-essential

# 암호화 라이브러리 (pandas, numpy 등에 필요)
sudo apt install -y libssl-dev libffi-dev

# 설치 확인
gcc --version
# gcc 버전 정보가 출력되면 성공
```

#### Step 6: 추가 유틸리티 설치 (권장)
```bash
# curl, wget (파일 다운로드용)
sudo apt install -y curl wget

# vim 또는 nano (텍스트 편집기)
sudo apt install -y vim nano

# tree (디렉토리 구조 확인용)
sudo apt install -y tree

# 한글 locale 설정 (한글 파일명 처리용)
sudo apt install -y language-pack-ko
sudo locale-gen ko_KR.UTF-8
```

#### Step 7: python 명령어 별칭 설정 (선택사항)
```bash
# python 명령어가 python3를 가리키도록 설정
echo 'alias python=python3' >> ~/.bashrc
echo 'alias pip=pip3' >> ~/.bashrc

# 설정 적용
source ~/.bashrc

# 확인
python --version
# Python 3.12.x 출력되면 성공
```

#### Step 8: 최종 확인
```bash
# 모든 설치 확인
python3 --version && \
pip3 --version && \
python3 -m venv --help > /dev/null && echo "venv: OK" && \
gcc --version | head -1

# 예상 출력:
# Python 3.12.x
# pip 24.0 from ...
# venv: OK
# gcc (Ubuntu ...) ...
```

---

### 🐙 Git 설치 및 설정

> 📦 **필수!** 코드 버전 관리와 협업을 위해 Git이 필요합니다.

#### Git이란?
Git은 소스 코드 버전 관리 시스템입니다. 코드 변경 이력을 추적하고, 협업을 쉽게 만들어줍니다.
GitHub는 Git 저장소를 웹에서 관리할 수 있는 플랫폼입니다.

#### Step 1: Git 설치
```bash
# Git 설치
sudo apt install -y git

# 설치 확인
git --version
# 예상 출력: git version 2.43.0 이상
```

#### Step 2: Git 사용자 정보 설정 (필수)
```bash
# 사용자 이름 설정 (GitHub 계정명 또는 실명)
git config --global user.name "Your Name"

# 이메일 설정 (GitHub 가입 이메일)
git config --global user.email "your.email@example.com"

# 설정 확인
git config --list | grep -E "user.name|user.email"
# 예상 출력:
# user.name=Your Name
# user.email=your.email@example.com
```

#### Step 3: 기본 에디터 설정 (선택사항)
```bash
# vim 사용 (기본값)
git config --global core.editor vim

# 또는 nano 사용 (초보자 추천)
git config --global core.editor nano

# 설정 확인
git config --global core.editor
```

#### Step 4: 설치 확인
```bash
# Git 버전 및 설정 확인
git --version
git config --list

# 간단한 테스트
git help
# Git 도움말이 출력되면 성공
```

---

### 🎯 프로젝트 환경 설정

> ⚠️ **전제 조건**:
> 1. ✅ Python 3.8+ 설치 완료
> 2. ✅ Git 설치 및 설정 완료
> 3. ✅ 프로젝트 디렉토리 위치: `/home/cocori2864/team-review-project`

#### Step 1: 프로젝트 디렉토리 확인
```bash
# 프로젝트 디렉토리로 이동
cd /home/cocori2864/team-review-project

# 현재 위치 확인
pwd
# 예상 출력: /home/cocori2864/team-review-project

# 프로젝트 파일 목록 확인
ls -la
# Python 스크립트 파일들(*.py), requirements.txt 등이 보여야 함
```

#### Step 2: 가상환경 생성 및 활성화
```bash
# 가상환경 생성 (필수!)
python3 -m venv venv

# 가상환경 활성화
source venv/bin/activate
# 프롬프트에 (venv) 표시되면 성공
# 예: (venv) user@hostname:~/team-review-project$

# 활성화 확인
which python
# 예상 출력: /home/cocori2864/team-review-project/venv/bin/python

# 가상환경 비활성화 (필요시)
# deactivate
```

#### Step 3: pip 업그레이드 및 패키지 설치
```bash
# pip 최신 버전으로 업그레이드
pip install --upgrade pip setuptools wheel

# requirements.txt 기반 패키지 일괄 설치 (권장)
pip install -r requirements.txt

# 또는 개별 패키지 설치
pip install pandas>=2.0.0
pip install openpyxl>=3.1.0
pip install plotly>=5.0.0
pip install tqdm>=4.0.0
pip install google-generativeai>=0.3.0
pip install numpy>=1.20.0

# 설치 확인
pip list | grep -E "pandas|plotly|openpyxl|tqdm|google-generativeai"
# 모든 패키지가 표시되어야 함
```

#### Step 4: 프로젝트 구조 확인 및 생성
```bash
# 현재 위치 확인
pwd
# 출력: /home/cocori2864/team-review-project

# 필수 폴더 존재 확인
ls -la

# 누락된 폴더 생성 (필요시)
mkdir -p rawdata checkpoints 개별보고서 src

# 폴더 구조 확인
tree -L 2
# 또는
ls -R

# 권한 확인 및 설정
chmod 755 *.py
chmod +x activate_venv.sh  # 있다면
```

#### Step 5: 환경 변수 설정 (선택사항)
```bash
# .bashrc 또는 .zshrc에 프로젝트 관련 환경변수 추가
cat >> ~/.bashrc << 'EOF'

# 협업평가 시스템 환경변수
export TEAM_REVIEW_HOME="/home/cocori2864/team-review-project"
export PYTHONPATH="${TEAM_REVIEW_HOME}:${PYTHONPATH}"
alias cdteam="cd ${TEAM_REVIEW_HOME}"
alias activate-team="cd ${TEAM_REVIEW_HOME} && source venv/bin/activate"
EOF

# 설정 적용
source ~/.bashrc

# 확인
echo $TEAM_REVIEW_HOME
cdteam  # 프로젝트 디렉토리로 이동
```

---

## 📦 데이터 준비 및 API 설정

### 데이터 파일 준비

#### Step 1: 데이터 파일 확인
```bash
# rawdata 폴더의 파일 확인
ls -lh rawdata/

# 필수 파일 체크리스트:
# ✓ 설문조사진행현황[VCRCRIC120S]_*.xlsx (여러 연도)
# ✓ 부서_부문_매핑.xlsx
# ✓ 부서명_표준화_매핑.xlsx (선택)

# 파일이 없으면 적절한 위치로 복사
# cp /path/to/source/* rawdata/
```

#### 옵션 A: 이미 처리된 데이터가 있는 경우
- `rawdata/2. text_processor_결과_*.xlsx` 확인
- → 바로 보고서 생성 가능!

#### 옵션 B: 새 데이터 수집이 필요한 경우
```bash
# 1단계: 데이터 수집 및 정제
python3 "0. 데이터_정리.py"

# 2단계: AI 감정 분석
python3 "0. 주관식_정제.py"
```

### Google Gemini API 인증 설정

#### Step 1: API 키 발급
1. https://makersuite.google.com/app/apikey 접속
2. API 키 생성
3. API 키 복사

#### Step 2: API 키 파일 생성
```bash
# Gemini API 키 파일 생성
cat > "Gemini API.json" << 'EOF'
{
  "apikey": "YOUR_API_KEY_HERE"
}
EOF

# 파일 권한 설정 (보안)
chmod 600 "Gemini API.json"

# 확인
cat "Gemini API.json"

# .gitignore에 추가되어 있는지 확인
grep "Gemini API.json" .gitignore
```

#### Step 3: 환경 변수 설정 (선택사항)
```bash
export GOOGLE_APPLICATION_CREDENTIALS="./Gemini API.json"
```

#### ⚠️ 중요 사항
- **API 키 필요**: Google Gemini API 키가 필요합니다
- **처리 시간**: 데이터량에 따라 1-3시간 소요될 수 있습니다
- **중단/재개 가능**: Ctrl+C로 중단 후 다시 실행하면 이어서 처리됩니다
- **API 사용량 제한**: Google Gemini API는 일일 사용량 제한이 있음

---

## 📁 프로젝트 구조 및 파일 설명

### 디렉토리 구조

```
team-review-project/
├── src/                            # 공통 모듈
│   ├── __init__.py
│   ├── common.py                   # 공통 유틸리티 함수
│   └── config.py                   # 설정 관리
│
├── 0. 데이터_정리.py                # 원본 데이터 수집 및 정제
├── 0. 주관식_정제.py                # AI 텍스트 감정 분석
├── 1. 대시보드_연도_반기.py          # 통합 대시보드 생성
├── 4. 연도별_통계분석.py            # 연도별 통계
├── 5. 반기별_통계분석.py            # 반기별 통계
├── 6. 상호평가_부서추출_연도_반기.py  # 부서별 데이터 추출
├── 7. 진단_데이터정제표.py          # 데이터 품질 진단
│
├── rawdata/                        # 원본 및 처리된 데이터
│   ├── 설문조사진행현황[VCRCRIC120S]_2022_1.xlsx
│   ├── 설문조사진행현황[VCRCRIC120S]_2023_1.xlsx
│   ├── 설문조사진행현황[VCRCRIC120S]_2023_2.xlsx
│   ├── 설문조사진행현황[VCRCRIC120S]_2024_1.xlsx
│   ├── 설문조사진행현황[VCRCRIC120S]_2025_1.xlsx
│   ├── 설문조사진행현황[VCRCRIC120S]_2025_2.xlsx
│   ├── 부서_부문_매핑.xlsx
│   ├── 부서명_표준화_매핑.xlsx
│   ├── 데이터_수정_이력.xlsx
│   ├── 1. data_processor_결과_*.xlsx
│   └── 2. text_processor_결과_*.xlsx
│
├── 개별보고서/                      # 부서별 보고서 출력
│   └── [부문명]/
│       └── [부서명].html
│
├── venv/                           # Python 가상환경
├── Gemini API.json                 # Google AI API 키 파일
└── README.md                       # 프로젝트 문서
```

### 핵심 파일 설명

#### 공통 모듈 (src/)

**src/common.py**
- 공통 유틸리티 함수
- 최신 파일 자동 감지
- 안전한 데이터 파싱
- 제외할 부문/부서 설정
- 점수 항목 정의

**src/config.py**
- 공통 설정 관리
- 파일 경로, 컬럼명 정의
- 점수 항목 표준화

#### 데이터 처리 스크립트

**0. 데이터_정리.py**
- 역할: Google Sheets 데이터 수집 및 전처리
- 기능: 부서-부문 매핑, 부서명 표준화, 점수 계산
- 입력: `rawdata/설문조사진행현황[VCRCRIC120S]_*.xlsx`
- 출력: `rawdata/1. data_processor_결과_*.xlsx`

**0. 주관식_정제.py**
- 역할: AI 기반 주관식 텍스트 분석
- 기능: Gemini API를 통한 감정 분석, 키워드 추출
- 입력: `rawdata/1. data_processor_결과_*.xlsx`
- 출력: `rawdata/2. text_processor_결과_*.xlsx`

#### 대시보드 생성

**1. 대시보드_연도_반기.py**
- 역할: 통합 대시보드 및 부서별 보고서 생성
- 기능:
  - 전체 병원 통합 대시보드
  - 반기별 분리 대시보드 (--split)
  - 특정 부서 보고서 (--department)
  - 전체 부서 개별 보고서 (--all-departments)
- 입력: `rawdata/2. text_processor_결과_*.xlsx`
- 출력: HTML 대시보드

#### 통계 분석

**4. 연도별_통계분석.py**
- 역할: 연도별 협업 평가 통계 분석
- 입력: `rawdata/2. text_processor_결과_*.xlsx`
- 출력: `연도별_통계분석.xlsx`

**5. 반기별_통계분석.py**
- 역할: 반기별 협업 평가 통계 분석
- 입력: `rawdata/2. text_processor_결과_*.xlsx`
- 출력: `반기별_통계분석.xlsx`

#### 데이터 추출 및 진단

**6. 상호평가_부서추출_연도_반기.py**
- 역할: 부서별 상호평가 데이터 추출
- 입력: `rawdata/2. text_processor_결과_*.xlsx`
- 출력: `상호평가_부서추출*.xlsx`

**7. 진단_데이터정제표.py**
- 역할: 데이터 품질 진단 및 검증
- 입력: `rawdata/2. text_processor_결과_*.xlsx`
- 출력: `진단_데이터정제표.xlsx`

---

## 🚀 사용 방법

### 빠른 시작 (5분)

#### 전제 조건
- Python 3.8 이상 설치 완료
- 가상환경 활성화 완료
- 처리된 데이터 파일 (`rawdata/2. text_processor_결과_*.xlsx`) 또는 원본 데이터

#### 실행 방법

```bash
# 가상환경 활성화 (필수!)
source venv/bin/activate

# 1️⃣ 전체 병원 통합 대시보드 (연도별)
python3 "1. 대시보드_연도_반기.py"
# 출력: 서울아산병원 협업평가 결과.html

# 2️⃣ 반기별 분할 대시보드
python3 "1. 대시보드_연도_반기.py" --split
# 출력: 협업평가 결과_반기별.html

# 3️⃣ 특정 부서 개별 보고서
python3 "1. 대시보드_연도_반기.py" \
  --department "암병원간호1팀" \
  --division "간호부문"
# 출력: 서울아산병원 협업평가 결과.html (해당 부서만)

# 4️⃣ 모든 부서 개별 보고서 일괄 생성
python3 "1. 대시보드_연도_반기.py" --all-departments
# 출력: 개별보고서/[부문명]/[부서명].html
```

### 보고서 생성 옵션

| 옵션 | 설명 | 예시 |
|------|------|------|
| (기본) | 전체 병원 통합 보고서 | `python3 "1. 대시보드_연도_반기.py"` |
| `--split` | 2025년 상하반기 분리 | `--split` |
| `--department` | 특정 부서 보고서 | `--department "부서명" --division "부문명"` |
| `--all-departments` | 모든 부서 개별 보고서 | `--all-departments` |

### 시나리오별 사용법

#### 시나리오 1: 전체 병원 통합 대시보드
```bash
python3 "1. 대시보드_연도_반기.py"
```

**출력**:
- `서울아산병원 협업평가 결과.html`
- 2025년 전체 기간 통합
- 약 10-15초 소요

**사용 상황**:
- 병원 전체 협업 현황을 한눈에 보고 싶을 때
- 경영진 보고용
- 연간 협업 트렌드 분석

#### 시나리오 2: 상반기/하반기 비교
```bash
python3 "1. 대시보드_연도_반기.py" --split
```

**출력**:
- `협업평가 결과_반기별.html`
- 2025년 상반기/하반기 구분 표시
- 약 10-15초 소요

**사용 상황**:
- 반기별 성과 비교
- 개선 추이 확인
- 계절적 변화 분석

#### 시나리오 3: 특정 부서 보고서
```bash
python3 "1. 대시보드_연도_반기.py" \
  --department "암병원간호1팀" \
  --division "간호부문"
```

**출력**:
- `서울아산병원 협업평가 결과.html` (해당 부서 데이터만)

**사용 상황**:
- 특정 부서 상세 분석
- 부서장 개별 리포트

#### 시나리오 4: 모든 부서 개별 보고서
```bash
python3 "1. 대시보드_연도_반기.py" --all-departments
```

**출력**:
- `개별보고서/[부문명]/[부서명].html`
- 약 1-2분 소요

**출력 구조 예시**:
```
개별보고서/
├── 간호부문/
│   ├── 암병원간호1팀.html
│   ├── 암병원간호2팀.html
│   └── ...
├── 진료부문/
│   ├── 영상의학팀.html
│   └── ...
└── 커뮤니케이션실/
    ├── 고객만족팀.html
    └── ...
```

**사용 상황**:
- 각 부서장에게 개별 보고서 전달
- 부서별 맞춤 분석

#### 시나리오 5: 통계 분석
```bash
# 연도별 통계
python3 "4. 연도별_통계분석.py"

# 반기별 통계
python3 "5. 반기별_통계분석.py"
```

**출력**:
- `연도별_통계분석.xlsx`
- `반기별_통계분석.xlsx`

**사용 상황**:
- 수치 기반 심층 분석
- Excel 기반 추가 분석

---

## 📊 스크립트별 상세 설명

### 1️⃣ **0. 데이터_정리.py**

**목적**: 원시 설문조사 엑셀 파일들을 읽어서 하나로 통합하고, 부서명을 표준화하여 부문 정보를 추가합니다.

**입력 파일**:
- `rawdata/설문조사진행현황[VCRCRIC120S]_*.xlsx` (설문 원시 데이터)
- `rawdata/부서명_표준화_매핑.xlsx` (선택사항)
- `rawdata/부서_부문_매핑.xlsx` (선택사항)

**출력 파일**:
- `rawdata/1. data_processor_결과_YYYYMMDD_HHMMSS.xlsx`

**주요 기능**:
1. 여러 설문조사 파일을 자동으로 찾아서 통합
2. 부서명 표준화 (예: "내과1" → "내과")
3. 부서-부문 매핑 (예: "내과" → "내과부문")
4. 점수를 100점 척도로 변환
5. 결측값 및 극단값 표시

**실행 방법**:
```bash
python3 "0. 데이터_정리.py"
```

**수정이 필요한 경우**:
- 매핑 파일 추가/수정: `rawdata/부서_부문_매핑.xlsx` 파일을 엑셀에서 직접 편집
- 제외할 부서 추가: `src/common.py` 파일의 `EXCLUDE_DEPARTMENTS` 또는 `EXCLUDE_TEAMS` 리스트에 추가

---

### 2️⃣ **0. 주관식_정제.py**

**목적**: 주관식 협업 후기를 Google Gemini AI를 사용해 감정분석하고 정제합니다.

**입력 파일**:
- `rawdata/1. data_processor_결과_*.xlsx` (자동으로 최신 파일 선택)
- `Gemini API.json` (API 키 파일)

**출력 파일**:
- `rawdata/2. text_processor_결과_YYYYMMDD_HHMMSS.xlsx`

**주요 기능**:
1. 주관식 답변의 감정 분석 (긍정/부정/중립)
2. 감정 강도 점수 계산 (1-10점)
3. 핵심 키워드 추출
4. 부적절한 표현 순화
5. 개인정보 비식별화

**실행 방법**:
```bash
python3 "0. 주관식_정제.py"
```

**⚠️ 중요 사항**:
- **API 키 필요**: Google Gemini API 키가 필요합니다
- **처리 시간**: 데이터량에 따라 1-3시간 소요될 수 있습니다
- **중단/재개 가능**: Ctrl+C로 중단 후 다시 실행하면 이어서 처리됩니다

---

### 3️⃣ **1. 대시보드_연도_반기.py**

**목적**: 분석 결과를 대화형 HTML 대시보드로 생성합니다.

**입력 파일**:
- `rawdata/2. text_processor_결과_*.xlsx` (자동으로 최신 파일 선택)

**출력 파일**:
- `서울아산병원 협업평가 결과.html` (기본)
- `협업평가 결과_반기별.html` (--split 옵션)
- `개별보고서/[부문명]/[부서명].html` (--all-departments 옵션)

**주요 기능**:
1. 병원 전체 결과 차트
2. 부문별 점수 비교
3. 부서별 상세 분석
4. 감정 분석 결과 시각화
5. 협업 네트워크 분석

**실행 방법**:
```bash
# 연도별 통합 모드 (기본)
python3 "1. 대시보드_연도_반기.py"

# 2025년 상하반기 구분 모드
python3 "1. 대시보드_연도_반기.py" --split

# 특정 부서 보고서
python3 "1. 대시보드_연도_반기.py" --department "부서명" --division "부문명"

# 모든 부서 개별 보고서
python3 "1. 대시보드_연도_반기.py" --all-departments
```

**대시보드 사용법**:
- 생성된 HTML 파일을 웹브라우저로 열기
- 각 차트는 대화형으로 필터링 가능
- 마우스 오버로 상세 정보 확인

---

### 4️⃣ **4. 연도별_통계분석.py**

**목적**: 연도별 통계를 엑셀 파일로 추출합니다.

**입력 파일**:
- `rawdata/2. text_processor_결과_*.xlsx`

**출력 파일**:
- `연도별_통계분석.xlsx`

**생성되는 시트**:
1. 연도별_문항별_점수: 각 연도의 문항별 평균 점수
2. 부문별_종합점수: 부문별 연도별 점수
3. 부문별_부서_종합점수: 부서별 상세 점수

**실행 방법**:
```bash
python3 "4. 연도별_통계분석.py"
```

---

### 5️⃣ **5. 반기별_통계분석.py**

**목적**: 2025년을 상하반기로 구분하여 통계를 추출합니다.

**입력 파일**:
- `rawdata/2. text_processor_결과_*.xlsx`

**출력 파일**:
- `반기별_통계분석.xlsx`

**특징**:
- 2022-2024년: 연도별로만 집계
- 2025년: 상반기/하반기 구분하여 집계

**실행 방법**:
```bash
python3 "5. 반기별_통계분석.py"
```

---

### 6️⃣ **6. 상호평가_부서추출_연도_반기.py**

**목적**: 부서 간 상호평가 결과를 추출합니다.

**입력 파일**:
- `rawdata/2. text_processor_결과_*.xlsx`

**출력 파일**:
- `상호평가_부서추출*.xlsx`

**주요 내용**:
- A부서가 B부서를 평가한 점수
- B부서가 A부서를 평가한 점수
- 상호평가 응답 수

**실행 방법**:
```bash
# 연도별 분석
python3 "6. 상호평가_부서추출_연도_반기.py"

# 반기별 분석 (2025년 상하반기 구분)
python3 "6. 상호평가_부서추출_연도_반기.py" --half-year
```

---

### 7️⃣ **7. 진단_데이터정제표.py**

**목적**: 데이터 정제 과정을 단계별로 추적하여 어떤 데이터가 제외되었는지 확인합니다.

**입력 파일**:
- `rawdata/2. text_processor_결과_*.xlsx`

**출력 파일**:
- `진단_데이터정제표_연도별.xlsx` 또는 `진단_데이터정제표_반기별.xlsx`

**진단 내용**:
- STEP 1: 부문 필터링 (미분류, 윤리경영실 제외)
- STEP 2: 부서 필터링 (내분비외과 제외)
- STEP 3: 종합점수 결측값 제거

**실행 방법**:
```bash
# 연도별 진단
python3 "7. 진단_데이터정제표.py"

# 반기별 진단 (2025년 구분)
python3 "7. 진단_데이터정제표.py" --split
```

---

## 📋 일반적인 작업 흐름

### 데이터 처리 흐름

```
원본 설문조사 파일 (rawdata/*.xlsx)
    ↓
0. 데이터_정리.py 실행
    ↓
부서-부문 매핑, 점수 계산, 데이터 정제
    ↓
1. data_processor_결과.xlsx 생성
    ↓
0. 주관식_정제.py 실행
    ↓
Gemini API로 텍스트 감정 분석
    ↓
2. text_processor_결과.xlsx 생성
    ↓
1. 대시보드_연도_반기.py 실행
    ↓
HTML 대시보드 생성
```

### 정기 설문조사 처리 (매 반기)

1. 새 설문 원시 파일을 `rawdata/` 폴더에 저장
2. 순서대로 실행:
   ```bash
   python3 "0. 데이터_정리.py"
   python3 "0. 주관식_정제.py"
   python3 "1. 대시보드_연도_반기.py"
   ```
3. 생성된 HTML 파일 확인

### 부서명 변경 시

1. `rawdata/부서_부문_매핑.xlsx` 파일 업데이트
2. `0. 데이터_정리.py`부터 다시 실행

### 특정 부서 제외하기

1. `src/common.py` 파일의 `EXCLUDE_TEAMS` 리스트 수정
2. 모든 스크립트 다시 실행

### 부서-부문 매핑 시스템

#### 매핑 파일 구조

**부서_부문_매핑.xlsx**
- 컬럼: `부문`, `부서명`, `소속UNIT`
- 역할: 부서를 부문으로 그룹화

**부서명_표준화_매핑.xlsx**
- 컬럼: `변경전_부서명`, `표준_부서명`
- 역할: 부서명 변경 이력 관리

#### 매핑 우선순위

1. **부서명 + Unit 완전 일치** → 해당 부문
2. **부서명만 일치 (Unit 없음)** → 기본 부문
3. **부서명 일치 + Unit 불일치** → '미분류'
4. **부서명 없음** → '미분류'

**예시**:
- 암병원간호1팀 + 62 Unit → 간호부문 ✅
- 암병원간호1팀 + (Unit 없음) → 간호부문 ✅
- 암병원간호1팀 + 999 Unit → 미분류 ⚠️

### 공통 설정 수정 방법

#### src/common.py 파일

모든 스크립트가 공통으로 사용하는 설정이 저장되어 있습니다.

**주요 설정**:
```python
# 제외할 부문
EXCLUDE_DEPARTMENTS = ['미분류', '윤리경영실']

# 제외할 부서
EXCLUDE_TEAMS = ['내분비외과']

# 점수 항목 정의
SCORE_COLUMNS = ['존중배려', '정보공유', '명확처리', '태도개선', '전반만족', '종합점수']
```

**수정 방법**:
1. 텍스트 편집기로 `src/common.py` 열기
2. 해당 줄 찾아서 수정
3. 저장 후 스크립트 다시 실행

### 유지보수 가이드

#### 대시보드 수정
→ `1. 대시보드_연도_반기.py` 수정

#### 공통 설정 변경
→ `src/config.py` 또는 `src/common.py` 수정

#### 새 데이터 수집
→ `0. 데이터_정리.py` → `0. 주관식_정제.py` 순서로 실행

#### 부서-부문 매핑 업데이트
→ `rawdata/부서_부문_매핑.xlsx` 수정 후 재실행

---

## ⚠️ 주의사항

1. **파일명 규칙 준수**
   - 원시 파일명: `설문조사진행현황[VCRCRIC120S]_YYYY_N.xlsx` 형식 유지
   - YYYY: 연도, N: 반기(1 또는 2)

2. **순서 준수**
   - 반드시 번호 순서대로 실행 (0 → 0 → 1)
   - 이전 단계 결과 파일이 다음 단계 입력이 됨

3. **API 사용량**
   - Google Gemini API는 일일 사용량 제한이 있음
   - 대량 처리 시 여러 날에 나눠서 처리 권장

4. **백업**
   - 중요한 작업 전 `rawdata/` 폴더 전체 백업 권장
   - 특히 매핑 파일들은 별도 보관

5. **파일명에 공백이 있어서 실행 안 됨**
   - 따옴표로 감싸기: `python3 "1. 대시보드_연도_반기.py"`

---

## ❓ 문제 해결 및 트러블슈팅

### 자주 발생하는 문제

#### 1. `ModuleNotFoundError: No module named 'xxx'`

**원인**: 패키지가 설치되지 않음

**해결**:
```bash
# 가상환경 활성화 확인
source venv/bin/activate

# 패키지 설치
pip install -r requirements.txt

# 또는 개별 설치
pip install pandas numpy plotly openpyxl tqdm google-generativeai
```

#### 2. Google Gemini API 인증 오류

**원인**: API 키 설정 문제

**해결**:
```bash
# Gemini API.json 파일 확인
ls -lh "Gemini API.json"

# 파일 내용 확인 (API 키가 있는지)
cat "Gemini API.json"

# 파일 권한 확인
chmod 600 "Gemini API.json"
```

#### 3. 파일을 찾을 수 없음

**원인**: `rawdata/` 폴더에 데이터 파일이 없음

**해결**:
```bash
# 파일 확인
ls rawdata/2.*text_processor*.xlsx

# 없으면 데이터 수집 실행
python3 "0. 데이터_정리.py"
python3 "0. 주관식_정제.py"
```

#### 4. 부서별 보고서 생성 실패

**원인**: 부서명 또는 부문명 오류

**해결**:
```bash
# 정확한 부서명 확인
python3 -c "
import pandas as pd
df = pd.read_excel('rawdata/부서_부문_매핑.xlsx')
print(df[['부문', '부서명']].drop_duplicates())
"
```

#### 5. 부서_부문_매핑.xlsx 관련 오류

**증상**: "암병원간호1팀 62 Unit을 찾을 수 없음"

**해결**:
```bash
# 매핑 파일 확인
python3 -c "
import pandas as pd
df = pd.read_excel('rawdata/부서_부문_매핑.xlsx')
result = df[df['부서명'].str.contains('암병원간호1팀', na=False)]
print(result)
"
```

### WSL 관련 (Windows 사용자)

#### 1. WSL 설치 오류
```powershell
# 증상: "wsl --install" 명령어가 작동하지 않음
# 해결: Windows 버전 확인 (빌드 19041 이상 필요)
winver

# 수동으로 WSL 기능 활성화
dism.exe /online /enable-feature /featurename:Microsoft-Windows-Subsystem-Linux /all /norestart
dism.exe /online /enable-feature /featurename:VirtualMachinePlatform /all /norestart

# 재부팅 후
wsl --set-default-version 2
wsl --install -d Ubuntu-24.04
```

#### 2. 가상화 오류
```
증상: "Please enable the Virtual Machine Platform Windows feature and ensure virtualization is enabled in the BIOS."

해결:
1. 작업 관리자 → 성능 탭 → 가상화: 사용 확인
2. 비활성화되어 있으면 BIOS 설정
   - 재부팅 → F2/F10/Del 키 (제조사마다 다름)
   - Virtualization Technology, Intel VT-x, AMD-V 찾아서 Enable
   - 저장 후 재부팅
```

#### 3. Ubuntu가 시작되지 않음
```powershell
# WSL 완전히 종료
wsl --shutdown

# Ubuntu 다시 시작
wsl -d Ubuntu-24.04

# 여전히 안되면 재설치
wsl --unregister Ubuntu-24.04
wsl --install -d Ubuntu-24.04
```

#### 4. 네트워크 연결 문제
```bash
# Ubuntu 터미널에서
# 증상: sudo apt update 시 연결 오류

# 해결: DNS 설정 변경
sudo rm /etc/resolv.conf
sudo bash -c 'echo "nameserver 8.8.8.8" > /etc/resolv.conf'
sudo bash -c 'echo "nameserver 8.8.4.4" >> /etc/resolv.conf'

# 확인
sudo apt update
```

### Ubuntu/Linux 시스템 관련

#### 1. Python 설치 오류
```bash
# 증상: python3 명령어가 없음
# 해결:
sudo apt update
sudo apt install -y python3 python3-pip

# 증상: pip install 시 permission denied
# 해결: 가상환경 사용 또는 --user 옵션
pip install --user package-name
# 또는 가상환경에서 설치 (권장)
source venv/bin/activate
pip install package-name
```

#### 2. 가상환경 활성화 오류
```bash
# 증상: source venv/bin/activate 실패
# 해결 1: python3-venv 패키지 설치
sudo apt install -y python3-venv

# 해결 2: 가상환경 재생성
rm -rf venv
python3 -m venv venv
source venv/bin/activate

# 증상: 프롬프트에 (venv) 표시 안됨
# 확인:
which python
# /home/cocori2864/team-review-project/venv/bin/python 이면 정상
```

#### 3. 패키지 컴파일 오류
```bash
# 증상: error: command 'gcc' failed
# 해결: 빌드 도구 설치
sudo apt install -y build-essential python3-dev

# 증상: fatal error: Python.h: No such file or directory
# 해결: python3-dev 설치
sudo apt install -y python3-dev

# 증상: libffi 관련 오류
# 해결:
sudo apt install -y libffi-dev libssl-dev
```

#### 4. 권한 오류
```bash
# 증상: Permission denied when accessing files
# 해결: 파일 소유권 확인 및 변경
ls -la
sudo chown -R $USER:$USER /home/cocori2864/team-review-project
chmod -R 755 /home/cocori2864/team-review-project

# 스크립트 실행 권한 부여
chmod +x *.py
chmod +x *.sh
```

#### 5. 메모리 부족
```bash
# 증상: Killed (메모리 부족으로 프로세스 종료)
# 확인:
free -h
df -h

# 해결: 스왑 메모리 확인 및 증설
sudo swapon --show
# 스왑이 없으면 생성 (2GB 예시)
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

#### 6. 인코딩 오류
```bash
# 증상: UnicodeDecodeError
# 해결: 환경변수 설정
export LANG=ko_KR.UTF-8
export LC_ALL=ko_KR.UTF-8

# 영구 적용
echo "export LANG=ko_KR.UTF-8" >> ~/.bashrc
echo "export LC_ALL=ko_KR.UTF-8" >> ~/.bashrc
source ~/.bashrc

# locale 설치 확인
locale -a | grep ko_KR

# 없으면 설치
sudo apt install -y language-pack-ko
sudo locale-gen ko_KR.UTF-8
```

### Python 관련

#### 일반 오류
```bash
# 모듈 찾기 오류
pip install -r requirements.txt

# 가상환경 확인
which python  # venv 경로여야 함

# PYTHONPATH 설정
export PYTHONPATH="/home/cocori2864/team-review-project:$PYTHONPATH"
```

#### 프로젝트 특화 오류
```bash
# 증상: ModuleNotFoundError: No module named 'src'
# 해결: PYTHONPATH 설정
export PYTHONPATH="/home/cocori2864/team-review-project:$PYTHONPATH"

# 증상: 데이터 파일을 찾을 수 없음
# 해결: 올바른 디렉토리에서 실행
cd /home/cocori2864/team-review-project
python3 "1. 대시보드_연도_반기.py"

# 증상: Gemini API 인증 오류
# 해결: API 키 파일 확인
ls -la "Gemini API.json"
cat "Gemini API.json"
```

### Git/GitHub 관련
```bash
# push 실패
git remote -v  # URL 확인
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# 충돌 발생
git pull origin main
# 충돌 해결 후
git add .
git commit -m "Resolve conflicts"
git push

# .gitignore 무시됨
git rm -r --cached .
git add .
git commit -m "Apply .gitignore"
```

### 성능 최적화

#### 메모리 부족 시

**증상**: `MemoryError` 또는 프로세스 중단

**해결**:
```python
# 불필요한 컬럼 제거
df = df[['필요한', '컬럼만']]

# 데이터 타입 최적화
df['컬럼명'] = df['컬럼명'].astype('category')
```

#### 느린 실행 속도

**해결**:
```bash
# --split 옵션 제거 (필요 없는 경우)
python3 "1. 대시보드_연도_반기.py"

# --all-departments 대신 특정 부서만
python3 "1. 대시보드_연도_반기.py" --department "부서명" --division "부문명"
```

---

## 📝 체크리스트

### 0단계: WSL 설치 체크리스트 (Windows 사용자 필수)
- [ ] Windows 버전 확인 (`winver` - 빌드 19041 이상)
- [ ] PowerShell 관리자 권한으로 실행
- [ ] WSL 설치 (`wsl --install -d Ubuntu-24.04`)
- [ ] 컴퓨터 재부팅
- [ ] Ubuntu 사용자 계정 생성 (영문 소문자)
- [ ] Ubuntu 버전 확인 (`lsb_release -a`)
- [ ] WSL 2 확인 (`wsl --list --verbose`)
- [ ] 시스템 업데이트 (`sudo apt update && sudo apt upgrade -y`)

### 1단계: Python 설치 체크리스트 (필수)
- [ ] 시스템 업데이트 (`sudo apt update && sudo apt upgrade -y`)
- [ ] Python 3 설치 (`sudo apt install -y python3`)
- [ ] pip 설치 (`sudo apt install -y python3-pip`)
- [ ] venv 설치 (`sudo apt install -y python3-venv`)
- [ ] 개발 도구 설치 (`sudo apt install -y python3-dev build-essential`)
- [ ] 암호화 라이브러리 설치 (`sudo apt install -y libssl-dev libffi-dev`)
- [ ] 추가 유틸리티 설치 (`sudo apt install -y curl wget vim tree`)
- [ ] 한글 locale 설치 (`sudo apt install -y language-pack-ko`)
- [ ] Python 별칭 설정 (선택, `alias python=python3`)
- [ ] 설치 확인 (`python3 --version && pip3 --version`)

### 2단계: Git 설치 체크리스트 (필수)
- [ ] Git 설치 (`sudo apt install -y git`)
- [ ] Git 사용자 이름 설정 (`git config --global user.name "Your Name"`)
- [ ] Git 이메일 설정 (`git config --global user.email "your@email.com"`)
- [ ] Git 에디터 설정 (선택, `git config --global core.editor nano`)
- [ ] 설치 확인 (`git --version && git config --list`)

### 3단계: 협업평가 시스템 환경 설정 (필수)
- [ ] 프로젝트 디렉토리로 이동 (`cd /home/cocori2864/team-review-project`)
- [ ] 프로젝트 파일 확인 (`ls -la`)
- [ ] 가상환경 생성 (`python3 -m venv venv`)
- [ ] 가상환경 활성화 (`source venv/bin/activate`)
- [ ] pip 업그레이드 (`pip install --upgrade pip setuptools wheel`)
- [ ] requirements.txt 패키지 설치 (`pip install -r requirements.txt`)
- [ ] 설치 확인 (`pip list | grep pandas`)

### 4단계: 프로젝트 구조 및 데이터 (필수)
- [ ] 필수 디렉토리 확인/생성 (`mkdir -p rawdata checkpoints 개별보고서 src`)
- [ ] rawdata 폴더에 설문조사 데이터 파일 배치
- [ ] 부서_부문_매핑.xlsx 파일 확인
- [ ] Gemini API 키 설정 (`Gemini API.json` 파일 생성)
- [ ] 스크립트 실행 권한 설정 (`chmod +x *.py *.sh`)

### 5단계: 첫 실행 테스트 (필수)
- [ ] 패키지 import 테스트 (`python3 -c "import pandas; import plotly"`)
- [ ] 데이터 정리 스크립트 실행 (`python3 "0. 데이터_정리.py"`)
- [ ] 대시보드 생성 테스트 (`python3 "1. 대시보드_연도_반기.py"`)
- [ ] 생성된 HTML 파일 확인 (`ls -lh *.html`)

### 선택: GitHub 연동 (선택사항)
- [ ] GitHub 계정 생성
- [ ] 로컬 Git 초기화 (`git init`)
- [ ] .gitignore 파일 확인
- [ ] 첫 커밋 (`git add . && git commit -m "Initial commit"`)
- [ ] GitHub 원격 저장소 생성
- [ ] 원격 저장소 연결 (`git remote add origin URL`)
- [ ] 푸시 (`git push -u origin main`)

### 일상 작업 체크리스트
- [ ] 가상환경 활성화 (`source venv/bin/activate`)
- [ ] 최신 코드 받기 (`git pull`, GitHub 사용 시)
- [ ] 코드 수정 및 테스트
- [ ] 변경사항 커밋 (`git add . && git commit -m "message"`)
- [ ] 원격 저장소 푸시 (`git push`, GitHub 사용 시)

---

## 📚 부록

### Ubuntu 빠른 참조 가이드

#### 자주 사용하는 명령어 모음

##### 시스템 관리
```bash
# 시스템 정보 확인
uname -a                    # 커널 버전
lsb_release -a              # Ubuntu 버전
free -h                     # 메모리 사용량
df -h                       # 디스크 사용량
top                         # 프로세스 모니터링

# 패키지 관리
sudo apt update             # 패키지 목록 업데이트
sudo apt upgrade            # 패키지 업그레이드
sudo apt search package     # 패키지 검색
sudo apt show package       # 패키지 정보
sudo apt autoremove         # 불필요한 패키지 제거
```

##### 파일 및 디렉토리
```bash
# 파일 탐색
ls -lah                     # 상세 목록 (숨김파일 포함)
tree -L 2                   # 트리 구조 (2단계)
find . -name "*.py"         # 파일 찾기
du -sh *                    # 디렉토리 크기

# 파일 작업
cp -r source dest           # 디렉토리 복사
mv old new                  # 이동/이름변경
rm -rf directory            # 디렉토리 삭제 (주의!)
chmod +x script.sh          # 실행 권한
chown user:user file        # 소유권 변경
```

##### Python 개발
```bash
# 가상환경
python3 -m venv venv        # 생성
source venv/bin/activate    # 활성화
deactivate                  # 비활성화
rm -rf venv                 # 삭제

# 패키지 관리
pip list                    # 설치된 패키지
pip freeze > req.txt        # 패키지 목록 저장
pip install -r req.txt      # 일괄 설치
pip show package            # 패키지 정보
pip uninstall package       # 제거

# 스크립트 실행
python3 script.py           # 실행
python3 -m module           # 모듈로 실행
python3 -c "print('hi')"    # 원라이너
```

##### Git 작업
```bash
# 상태 확인
git status                  # 변경사항
git log --oneline           # 커밋 이력
git diff                    # 차이점

# 기본 작업
git add .                   # 스테이징
git commit -m "message"     # 커밋
git push                    # 푸시
git pull                    # 풀

# 브랜치
git branch                  # 목록
git checkout -b feature     # 생성+전환
git merge feature           # 병합
git branch -d feature       # 삭제
```

#### 유용한 alias 설정
```bash
# ~/.bashrc에 추가
alias ll='ls -lah'
alias ..='cd ..'
alias ...='cd ../..'
alias gs='git status'
alias ga='git add'
alias gc='git commit'
alias gp='git push'
alias activate='source venv/bin/activate'
alias cdteam='cd /home/cocori2864/team-review-project'
alias rundb='cd /home/cocori2864/team-review-project && source venv/bin/activate && python3 "1. 대시보드_연도_반기.py"'

# 적용
source ~/.bashrc
```

#### 성능 최적화 팁
```bash
# 1. pip 캐시 정리
pip cache purge

# 2. apt 캐시 정리
sudo apt clean
sudo apt autoclean

# 3. 시스템 로그 정리
sudo journalctl --vacuum-time=7d

# 4. 스왑 사용 최적화
sudo sysctl vm.swappiness=10
echo "vm.swappiness=10" | sudo tee -a /etc/sysctl.conf

# 5. 불필요한 서비스 중지
sudo systemctl disable service-name
```

### 추가 학습 자료

#### Python
- [파이썬 공식 튜토리얼](https://docs.python.org/ko/3/tutorial/)
- [점프 투 파이썬](https://wikidocs.net/book/1)
- [파이썬 코딩 도장](https://dojang.io/course/view.php?id=7)

#### GitHub
- [GitHub 공식 가이드](https://docs.github.com/ko)
- [Git 공식 문서](https://git-scm.com/doc)
- [생활코딩 Git](https://opentutorials.org/course/3837)
- [Pro Git 책 (무료)](https://git-scm.com/book/ko/v2)

#### 개발 도구
- [Cursor 공식 문서](https://docs.cursor.sh)
- [VS Code 단축키](https://code.visualstudio.com/shortcuts/keyboard-shortcuts-windows.pdf)

---

## 📞 지원 및 문의

### 프로젝트 정보

- **프로젝트**: 서울아산병원 협업평가 시스템
- **버전**: 3.3
- **최종 업데이트**: 2025-10-22
- **Python 버전**: 3.8+

### 기술 스택

- **언어**: Python 3.8+
- **데이터 처리**: pandas, numpy
- **시각화**: plotly
- **AI**: Google Gemini API
- **환경**: WSL2 (Ubuntu), VS Code / Cursor

---

**문서 끝**

